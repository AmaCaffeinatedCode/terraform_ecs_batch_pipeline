name = "terraform_ecr_repo"

tags = {
  "Owner"   = "infrastructure-team"
  "Project" = "terraform_ecr_repo"
}

# "environment" tag is set dinamically in the CI/CD pipeline

# "project_url" tag is set dinamically in the CI/CD pipeline

region = "us-east-1"
